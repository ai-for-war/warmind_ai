---
phase: 01-meeting-domain-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config/settings.py
  - .env.example
  - app/common/event_socket.py
  - app/common/exceptions.py
  - app/domain/models/meeting_record.py
  - app/domain/schemas/meeting_record.py
  - app/repo/meeting_record_repo.py
  - app/common/repo.py
  - app/infrastructure/database/mongodb.py
  - tests/unit/domain/test_meeting_record_schema.py
  - tests/unit/repo/test_meeting_record_repo.py
autonomous: true
requirements:
  - MEET-01
  - MEET-03
user_setup: []
must_haves:
  truths:
    - "The public realtime meeting contract uses meeting_record:* events instead of stt:* or interview:* names."
    - "Meeting start payload validation requires organization_id, defaults language to en when omitted, and rejects unsupported languages before provider open."
    - "Meeting lifecycle persistence is stored in a meeting-native repository and collection, not in interview_conversations or interview_utterances."
  artifacts:
    - path: "app/domain/schemas/meeting_record.py"
      provides: "Meeting-native socket request and response payload schemas"
      min_lines: 80
      contains: "class MeetingRecordStartRequest"
    - path: "app/domain/models/meeting_record.py"
      provides: "Durable meeting lifecycle model and status enum"
      min_lines: 40
      contains: "class MeetingRecordStatus"
    - path: "app/repo/meeting_record_repo.py"
      provides: "Meeting record persistence API"
      min_lines: 60
      contains: "class MeetingRecordRepository"
  key_links:
    - from: "app/common/repo.py"
      to: "app/repo/meeting_record_repo.py"
      via: "repository factory"
      pattern: "get_meeting_record_repo"
    - from: "app/infrastructure/database/mongodb.py"
      to: "meeting_records"
      via: "MongoDB index creation"
      pattern: "meeting_records"
---

<objective>
Define the meeting-native contracts and durable artifacts that Phase 1 will execute against.

Purpose: Remove interview-specific public and persistence assumptions before lifecycle logic is implemented.
Output: Meeting settings, event names, errors, schemas, model, repository factory, and Mongo indexes ready for meeting lifecycle work.
</objective>

<execution_context>
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-meeting-domain-foundation/01-CONTEXT.md
@.planning/phases/01-meeting-domain-foundation/01-RESEARCH.md
@app/config/settings.py
@app/common/event_socket.py
@app/common/exceptions.py
@app/domain/schemas/stt.py
@app/domain/models/interview_conversation.py
@app/repo/interview_conversation_repo.py
@app/common/repo.py
@app/infrastructure/database/mongodb.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add meeting-specific configuration, events, and exceptions</name>
  <files>app/config/settings.py, .env.example, app/common/event_socket.py, app/common/exceptions.py</files>
  <read_first>app/config/settings.py, .env.example, app/common/event_socket.py, app/common/exceptions.py, app/domain/schemas/stt.py</read_first>
  <action>Add meeting-specific runtime settings in `app/config/settings.py`: `MEETING_STT_CHANNELS: int = 1`, `MEETING_STT_MULTICHANNEL: bool = False`, `MEETING_STT_ENDPOINTING_MS: int = 400`, `MEETING_STT_UTTERANCE_END_MS: int = 1000`, `MEETING_STT_KEEPALIVE_INTERVAL_SECONDS: int = 5`, and `MEETING_SUPPORTED_LANGUAGES: list[str] = ["en", "vi"]`. Mirror the new values in `.env.example` using the exact variable names. In `app/common/event_socket.py`, add a `MeetingRecordEvents` class with `START = "meeting_record:start"`, `AUDIO = "meeting_record:audio"`, `STOP = "meeting_record:stop"`, `STARTED = "meeting_record:started"`, `STOPPING = "meeting_record:stopping"`, `COMPLETED = "meeting_record:completed"`, and `ERROR = "meeting_record:error"`. In `app/common/exceptions.py`, add dedicated meeting errors named `InvalidMeetingRecordStateError`, `ActiveMeetingRecordConflictError`, `MeetingRecordOwnershipError`, and `UnsupportedMeetingLanguageError` with status codes aligned to the existing STT exception style.</action>
  <verify>Select-String -Path app/config/settings.py,.env.example,app/common/event_socket.py,app/common/exceptions.py -Pattern "MEETING_SUPPORTED_LANGUAGES|meeting_record:start|meeting_record:stopping|UnsupportedMeetingLanguageError"</verify>
  <acceptance_criteria>
    - app/config/settings.py contains `MEETING_STT_CHANNELS: int = 1`
    - app/config/settings.py contains `MEETING_SUPPORTED_LANGUAGES: list[str] = ["en", "vi"]`
    - .env.example contains `MEETING_SUPPORTED_LANGUAGES=["en","vi"]`
    - app/common/event_socket.py contains `class MeetingRecordEvents`
    - app/common/event_socket.py contains `START = "meeting_record:start"` and `STOPPING = "meeting_record:stopping"`
    - app/common/exceptions.py contains `class UnsupportedMeetingLanguageError`
  </acceptance_criteria>
  <done>Meeting-specific configuration, event names, and exception types exist with the exact values required by the Phase 1 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Create meeting-native domain models and socket schemas</name>
  <files>app/domain/models/meeting_record.py, app/domain/schemas/meeting_record.py, tests/unit/domain/test_meeting_record_schema.py</files>
  <read_first>app/domain/models/interview_conversation.py, app/domain/models/interview_utterance.py, app/domain/schemas/stt.py, tests/unit/__init__.py, .planning/phases/01-meeting-domain-foundation/01-CONTEXT.md</read_first>
  <action>Create `app/domain/models/meeting_record.py` with `MeetingRecordStatus` values `active`, `stopping`, `completed`, and `failed`, plus a `MeetingRecord` model containing `_id`, `user_id`, `organization_id`, `language`, `status`, `provider`, `started_at`, `updated_at`, `stopped_at`, `completed_at`, and `error_message`. Create `app/domain/schemas/meeting_record.py` with strict Pydantic schemas: `MeetingRecordStartRequest` containing `organization_id`, optional `language`, `encoding="linear16"`, `sample_rate=16000`, and `channels=1`; `MeetingRecordAudioMetadata` containing `meeting_id`, `sequence`, optional `timestamp_ms`, `encoding="linear16"`, `sample_rate=16000`, and `channels=1`; `MeetingRecordStopRequest` containing `meeting_id`; `MeetingRecordStartedPayload` containing `meeting_id`, `organization_id`, `language`, `status="active"`, `encoding`, `sample_rate`, and `channels`; `MeetingRecordStoppingPayload` containing `meeting_id`, `status="stopping"`; `MeetingRecordCompletedPayload` containing `meeting_id`, `status="completed"`; and `MeetingRecordErrorPayload` containing `meeting_id | None`, `error_code`, `error_message`, and `retryable`. Normalize incoming language values to lowercase and default to `en` when omitted. Add `tests/unit/domain/test_meeting_record_schema.py` covering required `organization_id`, default language fallback to `en`, lowercase normalization for `"VI"` -> `"vi"`, and rejection of invalid `channels` values other than `1`.</action>
  <verify>Select-String -Path app/domain/models/meeting_record.py,app/domain/schemas/meeting_record.py,tests/unit/domain/test_meeting_record_schema.py -Pattern "class MeetingRecordStatus|class MeetingRecordStartRequest|meeting_id|status=\"stopping\"|test_default_language_is_en|test_language_is_normalized_to_lowercase"</verify>
  <acceptance_criteria>
    - app/domain/models/meeting_record.py contains `class MeetingRecordStatus`
    - app/domain/models/meeting_record.py contains `status: MeetingRecordStatus = MeetingRecordStatus.ACTIVE`
    - app/domain/schemas/meeting_record.py contains `class MeetingRecordStartRequest`
    - app/domain/schemas/meeting_record.py contains `organization_id: str`
    - app/domain/schemas/meeting_record.py contains `channels: Literal[1] = 1`
    - app/domain/schemas/meeting_record.py contains `meeting_id: str`
    - app/domain/schemas/meeting_record.py contains `status: Literal["stopping"] = "stopping"`
    - tests/unit/domain/test_meeting_record_schema.py contains `def test_default_language_is_en`
  </acceptance_criteria>
  <done>Meeting payloads and models are meeting-native and expose no public `conversation_id`, `channel_map`, or interview role fields.</done>
</task>

<task type="auto">
  <name>Task 3: Add durable meeting repository wiring and indexes</name>
  <files>app/repo/meeting_record_repo.py, app/common/repo.py, app/infrastructure/database/mongodb.py, tests/unit/repo/test_meeting_record_repo.py</files>
  <read_first>app/repo/conversation_repo.py, app/repo/interview_conversation_repo.py, app/common/repo.py, app/infrastructure/database/mongodb.py, app/domain/models/meeting_record.py, tests/unit/__init__.py</read_first>
  <action>Create `app/repo/meeting_record_repo.py` with a `MeetingRecordRepository` over the `meeting_records` collection. Implement `create(...)`, `get_by_id(...)`, `mark_stopping(...)`, `mark_completed(...)`, and `mark_failed(...)` methods that always update `updated_at` and preserve `organization_id` scoping when supplied. Add `get_meeting_record_repo()` to `app/common/repo.py`. Extend `app/infrastructure/database/mongodb.py` to create `meeting_records` indexes named `idx_meeting_records_user_org_started` on `(user_id, organization_id, started_at DESC)` and `idx_meeting_records_status_org_started` on `(status, organization_id, started_at DESC)` so Phase 5 history queries and Phase 1 lifecycle lookups share the same collection design. Add `tests/unit/repo/test_meeting_record_repo.py` covering repository create, `mark_stopping`, `mark_completed`, and `mark_failed` transitions plus organization-scoped `get_by_id(...)` reads.</action>
  <verify>Select-String -Path app/repo/meeting_record_repo.py,app/common/repo.py,app/infrastructure/database/mongodb.py,tests/unit/repo/test_meeting_record_repo.py -Pattern "class MeetingRecordRepository|get_meeting_record_repo|idx_meeting_records_user_org_started|mark_stopping"</verify>
  <acceptance_criteria>
    - app/repo/meeting_record_repo.py contains `class MeetingRecordRepository`
    - app/repo/meeting_record_repo.py contains `async def mark_stopping`
    - app/repo/meeting_record_repo.py contains `async def mark_completed`
    - app/repo/meeting_record_repo.py contains `async def mark_failed`
    - app/common/repo.py contains `def get_meeting_record_repo() -> MeetingRecordRepository`
    - app/infrastructure/database/mongodb.py contains `idx_meeting_records_user_org_started`
    - app/infrastructure/database/mongodb.py contains `idx_meeting_records_status_org_started`
    - tests/unit/repo/test_meeting_record_repo.py contains `async def test_mark_completed_updates_status`
  </acceptance_criteria>
  <done>Meeting lifecycle persistence is wired through the repository layer and backed by explicit Mongo indexes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Select-String -Path app/config/settings.py,.env.example,app/common/event_socket.py,app/common/exceptions.py,app/domain/models/meeting_record.py,app/domain/schemas/meeting_record.py,app/repo/meeting_record_repo.py,app/common/repo.py,app/infrastructure/database/mongodb.py -Pattern "meeting_record:start|MEETING_SUPPORTED_LANGUAGES|MeetingRecordRepository"` returns matches
- [ ] `pytest tests/unit/domain/test_meeting_record_schema.py -q` passes
- [ ] `pytest tests/unit/repo/test_meeting_record_repo.py -q` passes
- [ ] No public meeting artifact references `conversation_id`, `channel_map`, `InterviewEvents`, or `interviewer` as a required meeting contract field
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Meeting contracts are clearly independent from interview contracts
- Durable meeting lifecycle storage exists before lifecycle execution work starts
  </success_criteria>

<output>
After completion, create `.planning/phases/01-meeting-domain-foundation/01-01-SUMMARY.md`
</output>
