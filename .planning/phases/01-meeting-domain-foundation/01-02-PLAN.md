---
phase: 01-meeting-domain-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/services/meeting/session.py
  - app/services/meeting/session_manager.py
  - app/services/meeting/meeting_service.py
  - app/common/service.py
  - app/socket_gateway/server.py
  - tests/unit/services/test_meeting_service.py
  - tests/integration/socket_gateway/test_meeting_record_socket.py
autonomous: true
requirements:
  - MEET-01
  - MEET-02
  - MEET-03
user_setup: []
must_haves:
  truths:
    - "A client can start exactly one active meeting per socket by sending meeting_record:start with organization_id and optional language."
    - "A started meeting can receive audio on meeting_record:audio using the returned meeting_id and can be stopped only by the user who started it."
    - "Stopping a meeting emits meeting_record:stopping before meeting_record:completed and finalizes the provider connection cleanly."
  artifacts:
    - path: "app/services/meeting/meeting_service.py"
      provides: "Meeting lifecycle orchestration with org, ownership, and language validation"
      min_lines: 90
      contains: "class MeetingService"
    - path: "app/services/meeting/session_manager.py"
      provides: "Process-local active meeting registry"
      min_lines: 100
      contains: "class MeetingSessionManager"
    - path: "app/socket_gateway/server.py"
      provides: "Socket handlers for meeting_record:start, meeting_record:audio, and meeting_record:stop"
      min_lines: 250
      contains: "MeetingRecordEvents"
  key_links:
    - from: "app/socket_gateway/server.py"
      to: "app/services/meeting/meeting_service.py"
      via: "socket handlers call service methods"
      pattern: "get_meeting_service|meeting_record"
    - from: "app/services/meeting/meeting_service.py"
      to: "app/repo/meeting_record_repo.py"
      via: "durable lifecycle state updates"
      pattern: "get_by_id|mark_stopping|mark_completed|mark_failed|create"
    - from: "app/services/meeting/session.py"
      to: "app/infrastructure/deepgram/client.py"
      via: "provider open/send_audio/finalize/close"
      pattern: "DeepgramLiveClient"
---

<objective>
Implement the live meeting session lifecycle on top of the new meeting-native contracts.

Purpose: Deliver the actual Phase 1 user behavior for meeting start, audio streaming, stop, auth and organization scoping, and language-aware provider setup.
Output: Meeting service and session manager, socket handlers, and tests covering happy path plus core failure modes.
</objective>

<execution_context>
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-meeting-domain-foundation/01-CONTEXT.md
@.planning/phases/01-meeting-domain-foundation/01-RESEARCH.md
@.planning/phases/01-meeting-domain-foundation/01-01-PLAN.md
@app/socket_gateway/server.py
@app/socket_gateway/auth.py
@app/common/service.py
@app/common/repo.py
@app/common/event_socket.py
@app/common/socket_payload_contract.py
@app/services/stt/session.py
@app/services/stt/session_manager.py
@app/services/stt/stt_service.py
@app/infrastructure/deepgram/client.py
@app/api/deps.py
@app/repo/organization_repo.py
@app/repo/organization_member_repo.py
@app/repo/meeting_record_repo.py
@app/domain/schemas/meeting_record.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement meeting lifecycle service and session manager</name>
  <files>app/services/meeting/session.py, app/services/meeting/session_manager.py, app/services/meeting/meeting_service.py, app/common/service.py</files>
  <read_first>app/services/stt/session.py, app/services/stt/session_manager.py, app/services/stt/stt_service.py, app/common/service.py, app/repo/meeting_record_repo.py, app/repo/organization_repo.py, app/repo/organization_member_repo.py, app/infrastructure/deepgram/client.py, app/config/settings.py, app/domain/schemas/meeting_record.py</read_first>
  <action>Create a new `app/services/meeting/` package with `MeetingSession`, `MeetingSessionManager`, and `MeetingService`. `MeetingSession` should own one live meeting with `meeting_id`, `sid`, `user_id`, `organization_id`, `language`, `state`, and a `DeepgramLiveClient` configured from the new `MEETING_STT_*` settings. `MeetingSessionManager` should keep one active meeting per socket, reject a second start with `ActiveMeetingRecordConflictError`, enforce meeting ownership by `meeting_id`, forward `meeting_record:audio` bytes, and handle `stop` and disconnect by running finalize then close. `MeetingService` should validate that `organization_id` exists and that the user is an active member before repository create, normalize language against `MEETING_SUPPORTED_LANGUAGES`, create the durable meeting record, start the session, mark the record `stopping` before provider finalize, and mark it `completed` or `failed` after close. Wire the new factories into `app/common/service.py` as `get_meeting_session_manager()` and `get_meeting_service()`.</action>
  <verify>Select-String -Path app/services/meeting/session.py,app/services/meeting/session_manager.py,app/services/meeting/meeting_service.py,app/common/service.py -Pattern "class MeetingSession|class MeetingSessionManager|class MeetingService|get_meeting_service"</verify>
  <acceptance_criteria>
    - app/services/meeting/session.py contains `class MeetingSession`
    - app/services/meeting/session_manager.py contains `class MeetingSessionManager`
    - app/services/meeting/session_manager.py contains `async def stop_session`
    - app/services/meeting/meeting_service.py contains `MEETING_SUPPORTED_LANGUAGES`
    - app/services/meeting/meeting_service.py contains `find_by_user_and_org`
    - app/services/meeting/meeting_service.py contains `mark_stopping` and `mark_completed`
    - app/common/service.py contains `def get_meeting_service() -> MeetingService`
  </acceptance_criteria>
  <done>Meeting lifecycle logic is isolated from interview code and enforces explicit organization, ownership, and language rules before and during runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Register meeting_record socket handlers and event ordering</name>
  <files>app/socket_gateway/server.py</files>
  <read_first>app/socket_gateway/server.py, app/socket_gateway/auth.py, app/common/event_socket.py, app/common/socket_payload_contract.py, app/domain/schemas/meeting_record.py, app/services/meeting/meeting_service.py</read_first>
  <action>Extend `app/socket_gateway/server.py` with new handlers for `MeetingRecordEvents.START`, `MeetingRecordEvents.AUDIO`, and `MeetingRecordEvents.STOP`. Parse payloads with the meeting schemas from `app/domain/schemas/meeting_record.py`. On start, call `MeetingService.start_session(...)` using the explicit `organization_id` from the request, persist that organization into the socket session only after start succeeds, and emit `meeting_record:started` with the full ready payload. On audio, require the owning meeting id and forward bytes to the meeting service without any `conversation_id` or `channel_map` checks. On stop, emit `meeting_record:stopping` first, then only emit `meeting_record:completed` after the service finalizes and closes the provider session. Route all meeting errors through `MeetingRecordEvents.ERROR`, and do not reuse `InterviewEvents` or `STTEvents` for public meeting responses.</action>
  <verify>Select-String -Path app/socket_gateway/server.py -Pattern "MeetingRecordEvents.START|MeetingRecordEvents.AUDIO|MeetingRecordEvents.STOP|meeting_record:stopping|_get_meeting_service"</verify>
  <acceptance_criteria>
    - app/socket_gateway/server.py contains `@sio.on(MeetingRecordEvents.START)`
    - app/socket_gateway/server.py contains `@sio.on(MeetingRecordEvents.AUDIO)`
    - app/socket_gateway/server.py contains `@sio.on(MeetingRecordEvents.STOP)`
    - app/socket_gateway/server.py contains `meeting_record:stopping` or `MeetingRecordEvents.STOPPING`
    - app/socket_gateway/server.py does not require `conversation_id` for meeting handlers
    - app/socket_gateway/server.py does not reference `STTChannelMap` inside meeting handlers
  </acceptance_criteria>
  <done>The public socket contract for meetings is fully registered and preserves the required event order from start through stop completion.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit and integration coverage for meeting start, stop, and rejection paths</name>
  <files>tests/unit/services/test_meeting_service.py, tests/integration/socket_gateway/test_meeting_record_socket.py</files>
  <read_first>tests/unit/__init__.py, tests/integration/__init__.py, app/services/meeting/meeting_service.py, app/socket_gateway/server.py, app/domain/schemas/meeting_record.py, .planning/phases/01-meeting-domain-foundation/01-VALIDATION.md</read_first>
  <action>Create `tests/unit/services/test_meeting_service.py` with async tests covering: default language fallback to `en`, rejection of unsupported languages outside `["en", "vi"]`, rejection when `organization_id` is invalid or membership is inactive, duplicate active start rejection on the same socket, and stop ownership enforcement. Create `tests/integration/socket_gateway/test_meeting_record_socket.py` with socket-level tests that assert the emitted order `meeting_record:started` before audio, `meeting_record:stopping` before `meeting_record:completed`, and `meeting_record:error` for invalid org or invalid language start attempts. Use fakes or stubs for `DeepgramLiveClient` so tests never require network access.</action>
  <verify>pytest tests/unit/services/test_meeting_service.py -q; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pytest tests/integration/socket_gateway/test_meeting_record_socket.py -q</verify>
  <acceptance_criteria>
    - tests/unit/services/test_meeting_service.py contains `unsupported language`
    - tests/unit/services/test_meeting_service.py contains `organization_id`
    - tests/integration/socket_gateway/test_meeting_record_socket.py contains `meeting_record:stopping`
    - tests/integration/socket_gateway/test_meeting_record_socket.py contains `meeting_record:completed`
    - Both test commands exit 0
  </acceptance_criteria>
  <done>Meeting lifecycle behavior is covered by automated tests for the phase's start, stop, language, and organization rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/services/test_meeting_service.py -q` passes
- [ ] `pytest tests/integration/socket_gateway/test_meeting_record_socket.py -q` passes
- [ ] `pytest -q` passes
- [ ] Meeting handlers emit `meeting_record:stopping` before `meeting_record:completed`
- [ ] Meeting handlers and services expose no public dependency on `conversation_id`, `channel_map`, or `InterviewEvents`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Meeting lifecycle can start, stream, and stop without touching interview-specific public contracts
- Organization and language validation fail before any meeting shell is created
  </success_criteria>

<output>
After completion, create `.planning/phases/01-meeting-domain-foundation/01-02-SUMMARY.md`
</output>
