---
phase: 02-live-transcript-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/common/event_socket.py
  - app/config/settings.py
  - .env.example
  - app/infrastructure/deepgram/client.py
  - app/domain/schemas/meeting_transcript.py
  - app/services/meeting/session.py
  - app/services/meeting/session_manager.py
  - app/services/meeting/meeting_service.py
  - app/socket_gateway/server.py
  - tests/unit/test_deepgram_live_client.py
  - tests/unit/test_meeting_transcript_session.py
  - tests/integration/socket_gateway/test_meeting_record_socket.py
autonomous: true
requirements:
  - TRNS-01
  - TRNS-03
  - TRNS-04
user_setup: []
must_haves:
  truths:
    - "The meeting runtime emits `meeting_record:transcript` updates that rewrite the active utterance in place and mark stable utterances with `is_final=true`."
    - "Anonymous speaker labels are derived from provider diarization data as `speaker {n+1}` and fall back to `speaker unknown` when diarization is missing."
    - "Stopping a meeting enters a listener-driven finalize flow that drains final transcript events before `meeting_record:completed` is emitted."
  artifacts:
    - path: "app/domain/schemas/meeting_transcript.py"
      provides: "Meeting-native live transcript socket payload schema"
      min_lines: 60
      contains: "class MeetingTranscriptUpdatePayload"
    - path: "app/services/meeting/session.py"
      provides: "Meeting transcript assembly and stable utterance closure logic"
      min_lines: 220
      contains: "MeetingSessionEventKind.TRANSCRIPT"
    - path: "app/socket_gateway/server.py"
      provides: "Meeting listener task and transcript event emission"
      min_lines: 320
      contains: "MeetingRecordEvents.TRANSCRIPT"
  key_links:
    - from: "app/socket_gateway/server.py"
      to: "app/services/meeting/meeting_service.py"
      via: "meeting listener loop collects provider-driven transcript events"
      pattern: "collect_session_events|meeting_record:transcript"
    - from: "app/services/meeting/session.py"
      to: "app/infrastructure/deepgram/client.py"
      via: "normalized provider transcript events become meeting transcript updates"
      pattern: "ProviderTranscriptEvent|speaker"
    - from: "app/services/meeting/session_manager.py"
      to: "app/services/meeting/session.py"
      via: "manager drains finalize and close events before session teardown"
      pattern: "_collect_session_events|request_finalize|close"
---

<objective>
Build the live meeting transcript event pipeline on top of the existing meeting session lifecycle.

Purpose: Deliver active-session transcript accumulation with anonymous speaker grouping and timestamped stable utterance closures, without leaking interview-specific semantics.
Output: Meeting transcript socket events, provider speaker normalization, transcript-aware meeting session management, and tests covering live rewrite plus finalize behavior.
</objective>

<execution_context>
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-transcript-capture/02-CONTEXT.md
@.planning/phases/02-live-transcript-capture/02-RESEARCH.md
@.planning/phases/02-live-transcript-capture/02-VALIDATION.md
@app/common/event_socket.py
@app/config/settings.py
@app/infrastructure/deepgram/client.py
@app/services/meeting/session.py
@app/services/meeting/session_manager.py
@app/services/meeting/meeting_service.py
@app/services/stt/session.py
@app/services/stt/session_manager.py
@app/socket_gateway/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add meeting transcript event contracts and provider speaker normalization</name>
  <files>app/common/event_socket.py, app/config/settings.py, .env.example, app/infrastructure/deepgram/client.py, app/domain/schemas/meeting_transcript.py</files>
  <read_first>app/common/event_socket.py, app/config/settings.py, .env.example, app/infrastructure/deepgram/client.py, app/domain/schemas/meeting_record.py, .planning/phases/02-live-transcript-capture/02-CONTEXT.md, .planning/phases/02-live-transcript-capture/02-RESEARCH.md</read_first>
  <action>In `app/common/event_socket.py`, add `MeetingRecordEvents.TRANSCRIPT = "meeting_record:transcript"`. In `app/config/settings.py`, add `MEETING_STT_DIARIZE: bool = True`, then mirror that exact variable in `.env.example`. Extend `DeepgramLiveClient` in `app/infrastructure/deepgram/client.py` so the constructor accepts `diarize`, `get_runtime_config()` includes `"diarize": "true"` for meetings, and the existing live-transcription config remains aligned with official Deepgram Nova streaming docs by keeping `interim_results`, `endpointing`, `utterance_end_ms`, and `vad_events`. Update `ProviderTranscriptEvent` to expose `speaker: int | None` plus `speaker_sequence: list[int] | None` derived from transcript-word speaker metadata. Create `app/domain/schemas/meeting_transcript.py` with `MeetingTranscriptUpdatePayload` fields `meeting_id`, `utterance_id`, `sequence`, `speaker_key`, `speaker_label`, `text`, `is_final`, `start_ms`, and `end_ms`.</action>
  <verify>Select-String -Path app/common/event_socket.py,app/config/settings.py,.env.example,app/infrastructure/deepgram/client.py,app/domain/schemas/meeting_transcript.py -Pattern "meeting_record:transcript|MEETING_STT_DIARIZE|speaker: int \\| None|interim_results|utterance_end_ms|class MeetingTranscriptUpdatePayload"</verify>
  <acceptance_criteria>
    - app/common/event_socket.py contains `TRANSCRIPT = "meeting_record:transcript"`
    - app/config/settings.py contains `MEETING_STT_DIARIZE: bool = True`
    - .env.example contains `MEETING_STT_DIARIZE=`
    - app/infrastructure/deepgram/client.py contains `"diarize": "true"`
    - app/infrastructure/deepgram/client.py contains `interim_results`
    - app/infrastructure/deepgram/client.py contains `utterance_end_ms`
    - app/infrastructure/deepgram/client.py contains `speaker: int | None`
    - app/domain/schemas/meeting_transcript.py contains `class MeetingTranscriptUpdatePayload`
    - app/domain/schemas/meeting_transcript.py contains `speaker_label: str`
    - app/domain/schemas/meeting_transcript.py contains `is_final: bool`
  </acceptance_criteria>
  <done>Meeting transcript socket payloads and provider-normalized diarization data exist with exact field names required for downstream live transcript assembly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement meeting transcript assembly, finalize draining, and socket listener wiring</name>
  <files>app/services/meeting/session.py, app/services/meeting/session_manager.py, app/services/meeting/meeting_service.py, app/socket_gateway/server.py</files>
  <read_first>app/services/meeting/session.py, app/services/meeting/session_manager.py, app/services/meeting/meeting_service.py, app/services/stt/session.py, app/services/stt/session_manager.py, app/socket_gateway/server.py, app/domain/schemas/meeting_transcript.py, app/infrastructure/deepgram/client.py, .planning/phases/02-live-transcript-capture/02-RESEARCH.md</read_first>
  <action>Expand `app/services/meeting/session.py` so `MeetingSessionEventKind` includes `TRANSCRIPT = "transcript"` and `MeetingSession` owns live transcript state for one open utterance at a time: `utterance_id`, `sequence`, `speaker_key`, `speaker_label`, `stable_text_cache`, `preview_text`, `start_ms`, and `end_ms`. On provider partial fragments, emit `MeetingTranscriptUpdatePayload` with `is_final=False` and rewrite the current utterance text in place. On provider final fragments, update the same utterance, keep millisecond timing, and assign `speaker_label` as `f"speaker {speaker + 1}"` when `speaker` exists, otherwise `speaker unknown`. Close and emit the stable utterance when Deepgram produces an utterance-end/finalize boundary. In `app/services/meeting/session_manager.py`, add `collect_session_events(...)`, `_collect_session_events(...)`, and finalize handling patterned after the interview `STTSessionManager`, so stop requests return `MeetingRecordStoppingPayload` immediately, then the listener drains final transcript events and only then emits `MeetingRecordCompletedPayload`. In `app/services/meeting/meeting_service.py`, expose `collect_session_events(...)` and keep stop/disconnect flows aligned with the listener-driven finalize sequence. In `app/socket_gateway/server.py`, add `_meeting_listener_tasks`, `_ensure_meeting_listener_task(...)`, `_cancel_meeting_listener(...)`, and `_run_meeting_listener(...)`, then map `MeetingSessionEventKind.TRANSCRIPT` to `MeetingRecordEvents.TRANSCRIPT` in `_emit_meeting_session_events(...)`.</action>
  <verify>Select-String -Path app/services/meeting/session.py,app/services/meeting/session_manager.py,app/services/meeting/meeting_service.py,app/socket_gateway/server.py -Pattern "MeetingSessionEventKind.TRANSCRIPT|collect_session_events|_meeting_listener_tasks|MeetingRecordEvents.TRANSCRIPT|speaker unknown|is_final"</verify>
  <acceptance_criteria>
    - app/services/meeting/session.py contains `MeetingSessionEventKind.TRANSCRIPT`
    - app/services/meeting/session.py contains `speaker unknown`
    - app/services/meeting/session.py contains `stable_text_cache`
    - app/services/meeting/session_manager.py contains `async def collect_session_events`
    - app/services/meeting/session_manager.py contains `request_finalize`
    - app/services/meeting/meeting_service.py contains `async def collect_session_events`
    - app/socket_gateway/server.py contains `_meeting_listener_tasks`
    - app/socket_gateway/server.py contains `_run_meeting_listener`
    - app/socket_gateway/server.py contains `MeetingRecordEvents.TRANSCRIPT`
    - app/socket_gateway/server.py emits `MeetingRecordEvents.STOPPING` before `MeetingRecordEvents.COMPLETED`
  </acceptance_criteria>
  <done>Meeting runtime can stream live transcript updates, preserve anonymous speaker grouping, and flush final utterances before terminal completion.</done>
</task>

<task type="auto">
  <name>Task 3: Add live transcript provider, session, and socket integration tests</name>
  <files>tests/unit/test_deepgram_live_client.py, tests/unit/test_meeting_transcript_session.py, tests/integration/socket_gateway/test_meeting_record_socket.py</files>
  <read_first>tests/unit/test_deepgram_live_client.py, tests/unit/test_stt_session.py, tests/integration/socket_gateway/test_meeting_record_socket.py, app/infrastructure/deepgram/client.py, app/services/meeting/session.py, app/socket_gateway/server.py, .planning/phases/02-live-transcript-capture/02-VALIDATION.md</read_first>
  <action>Extend `tests/unit/test_deepgram_live_client.py` to assert meeting runtime config includes `diarize=true`, preserves the documented live-transcription controls `interim_results`, `endpointing`, and `utterance_end_ms`, and normalizes provider transcript events with `speaker`. Create `tests/unit/test_meeting_transcript_session.py` with async tests covering live partial rewrite-in-place, fallback `speaker unknown`, stable utterance closure with `start_ms` and `end_ms`, and finalize-drain behavior before session completion. Extend `tests/integration/socket_gateway/test_meeting_record_socket.py` so a fake meeting provider stream produces the event sequence `meeting_record:started`, one or more `meeting_record:transcript`, `meeting_record:stopping`, optional final `meeting_record:transcript`, and `meeting_record:completed`, while confirming transcript payloads expose `meeting_id`, `utterance_id`, `speaker_label`, `text`, `is_final`, `start_ms`, and `end_ms` and never require `conversation_id`.</action>
  <verify>pytest tests/unit/test_deepgram_live_client.py tests/unit/test_meeting_transcript_session.py -q; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pytest tests/integration/socket_gateway/test_meeting_record_socket.py -q</verify>
  <acceptance_criteria>
    - tests/unit/test_deepgram_live_client.py contains `diarize`
    - tests/unit/test_deepgram_live_client.py contains `utterance_end_ms`
    - tests/unit/test_meeting_transcript_session.py contains `speaker unknown`
    - tests/unit/test_meeting_transcript_session.py contains `is_final`
    - tests/integration/socket_gateway/test_meeting_record_socket.py contains `meeting_record:transcript`
    - tests/integration/socket_gateway/test_meeting_record_socket.py contains `meeting_record:stopping`
    - tests/integration/socket_gateway/test_meeting_record_socket.py contains `meeting_record:completed`
    - All listed test commands exit 0
  </acceptance_criteria>
  <done>Phase 2 live transcript behavior is covered at the provider, session, and socket runtime layers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Select-String -Path app/common/event_socket.py,app/infrastructure/deepgram/client.py,app/domain/schemas/meeting_transcript.py,app/services/meeting/session.py,app/socket_gateway/server.py -Pattern "meeting_record:transcript|speaker unknown|is_final|collect_session_events"` returns matches
- [ ] `pytest tests/unit/test_deepgram_live_client.py tests/unit/test_meeting_transcript_session.py -q` passes
- [ ] `pytest tests/integration/socket_gateway/test_meeting_record_socket.py -q` passes
- [ ] Meeting transcript payloads do not expose `conversation_id`, `channel_map`, or interview role names as required public fields
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Live meeting transcript updates can be streamed without leaking interview-specific public contracts
- Stop/finalize preserves final transcript output before terminal completion
  </success_criteria>

<output>
After completion, create `.planning/phases/02-live-transcript-capture/02-01-SUMMARY.md`
</output>
