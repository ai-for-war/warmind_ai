---
phase: 02-live-transcript-capture
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/domain/models/meeting_transcript_item.py
  - app/domain/schemas/meeting_transcript.py
  - app/repo/meeting_transcript_repo.py
  - app/common/repo.py
  - app/infrastructure/database/mongodb.py
  - app/services/meeting/transcript_service.py
  - app/services/meeting/meeting_service.py
  - app/common/service.py
  - app/api/v1/meetings/router.py
  - app/api/v1/router.py
  - tests/unit/repo/test_meeting_transcript_repo.py
  - tests/unit/services/test_meeting_transcript_service.py
  - tests/integration/api/test_meeting_transcript_api.py
autonomous: true
requirements:
  - TRNS-01
  - TRNS-02
  - TRNS-03
  - TRNS-04
user_setup: []
must_haves:
  truths:
    - "Finalized meeting transcript segments are durably stored in a meeting-native transcript repository keyed by `meeting_id` and `organization_id`, while `block_id` preserves live block membership."
    - "`GET /meetings/{meeting_id}/transcript` returns oldest-first paginated transcript segments with `block_id`, `segment_id`, anonymous speaker labels, and millisecond timestamps for both active and completed meetings."
    - "Saved transcript review never exposes interview-only fields such as `conversation_id`, `channel`, or `source=\"interviewer\"`."
  artifacts:
    - path: "app/domain/models/meeting_transcript_item.py"
      provides: "Meeting-native durable transcript item model"
      min_lines: 40
      contains: "class MeetingTranscriptItem"
    - path: "app/repo/meeting_transcript_repo.py"
      provides: "Meeting transcript persistence and pagination API"
      min_lines: 90
      contains: "class MeetingTranscriptRepository"
    - path: "app/api/v1/meetings/router.py"
      provides: "Transcript review endpoint for meeting sessions"
      min_lines: 70
      contains: "@router.get(\"/{meeting_id}/transcript\""
  key_links:
    - from: "app/api/v1/meetings/router.py"
      to: "app/services/meeting/transcript_service.py"
      via: "router calls paginated transcript read service"
      pattern: "get_transcript_page"
    - from: "app/services/meeting/transcript_service.py"
      to: "app/repo/meeting_transcript_repo.py"
      via: "service persists closed block segments and serves transcript pages"
      pattern: "upsert_block_segments|list_by_meeting"
    - from: "app/services/meeting/meeting_service.py"
      to: "app/services/meeting/transcript_service.py"
      via: "final transcript events are persisted before they are exposed as durable reads"
      pattern: "append_closed_block|is_final"
---

<objective>
Persist finalized meeting transcript segments and expose transcript review paths for active and completed meetings.

Purpose: Turn closed transcript blocks from Plan 02-01 into durable saved review that satisfies transcript history, pagination, anonymous speaker grouping, and timestamp requirements.
Output: Meeting transcript model and repository, meeting transcript service, paginated transcript API, and automated tests for persistence plus read behavior.
</objective>

<execution_context>
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-transcript-capture/02-CONTEXT.md
@.planning/phases/02-live-transcript-capture/02-RESEARCH.md
@.planning/phases/02-live-transcript-capture/02-VALIDATION.md
@.planning/phases/02-live-transcript-capture/02-01-PLAN.md
@app/api/deps.py
@app/api/v1/router.py
@app/domain/schemas/meeting_transcript.py
@app/repo/meeting_record_repo.py
@app/repo/interview_utterance_repo.py
@app/common/repo.py
@app/common/service.py
@app/infrastructure/database/mongodb.py
@app/services/meeting/meeting_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create durable meeting transcript model, repository, and indexes</name>
  <files>app/domain/models/meeting_transcript_item.py, app/repo/meeting_transcript_repo.py, app/common/repo.py, app/infrastructure/database/mongodb.py</files>
  <read_first>app/domain/models/interview_utterance.py, app/repo/interview_utterance_repo.py, app/repo/meeting_record_repo.py, app/common/repo.py, app/infrastructure/database/mongodb.py, .planning/phases/02-live-transcript-capture/02-RESEARCH.md</read_first>
  <action>Create `app/domain/models/meeting_transcript_item.py` with a `MeetingTranscriptItem` model containing `_id`, `meeting_id`, `organization_id`, `block_id`, `segment_id`, `block_sequence`, `segment_index`, `speaker_key`, `speaker_label`, `text`, `start_ms`, `end_ms`, `created_at`, and `updated_at`. Create `app/repo/meeting_transcript_repo.py` with `MeetingTranscriptRepository` over a `meeting_transcript_items` collection and methods `upsert_block_segments(...)`, `list_by_meeting(...)`, and `count_by_meeting(...)`. Use `(block_sequence, segment_index)` as the oldest-first pagination order and make `upsert_block_segments(...)` idempotent by `meeting_id + segment_id`. Add `get_meeting_transcript_repo()` to `app/common/repo.py`. Extend `app/infrastructure/database/mongodb.py` to create a unique index named `idx_meeting_transcript_meeting_segment` on `(meeting_id, segment_id)` and a read index named `idx_meeting_transcript_org_meeting_sequence` on `(organization_id, meeting_id, block_sequence ASC, segment_index ASC)`.</action>
  <verify>Select-String -Path app/domain/models/meeting_transcript_item.py,app/repo/meeting_transcript_repo.py,app/common/repo.py,app/infrastructure/database/mongodb.py -Pattern "class MeetingTranscriptItem|class MeetingTranscriptRepository|upsert_block_segments|list_by_meeting|idx_meeting_transcript_meeting_segment|idx_meeting_transcript_org_meeting_sequence"</verify>
  <acceptance_criteria>
    - app/domain/models/meeting_transcript_item.py contains `class MeetingTranscriptItem`
    - app/domain/models/meeting_transcript_item.py contains `meeting_id: str`
    - app/domain/models/meeting_transcript_item.py contains `block_id: str`
    - app/domain/models/meeting_transcript_item.py contains `segment_id: str`
    - app/domain/models/meeting_transcript_item.py contains `speaker_label: str`
    - app/domain/models/meeting_transcript_item.py contains `start_ms: int`
    - app/domain/models/meeting_transcript_item.py contains `end_ms: int`
    - app/repo/meeting_transcript_repo.py contains `class MeetingTranscriptRepository`
    - app/repo/meeting_transcript_repo.py contains `async def upsert_block_segments`
    - app/repo/meeting_transcript_repo.py contains `async def list_by_meeting`
    - app/common/repo.py contains `def get_meeting_transcript_repo() -> MeetingTranscriptRepository`
    - app/infrastructure/database/mongodb.py contains `idx_meeting_transcript_meeting_segment`
    - app/infrastructure/database/mongodb.py contains `idx_meeting_transcript_org_meeting_sequence`
  </acceptance_criteria>
  <done>Finalized meeting transcript segments have a dedicated persistence model, repository, and query indexes aligned to paginated transcript review.</done>
</task>

<task type="auto">
  <name>Task 2: Persist closed meeting transcript blocks and expose transcript read services</name>
  <files>app/domain/schemas/meeting_transcript.py, app/services/meeting/transcript_service.py, app/services/meeting/meeting_service.py, app/common/service.py</files>
  <read_first>app/domain/schemas/meeting_transcript.py, app/services/meeting/meeting_service.py, app/common/service.py, app/repo/meeting_transcript_repo.py, app/repo/meeting_record_repo.py, .planning/phases/02-live-transcript-capture/02-01-PLAN.md, .planning/phases/02-live-transcript-capture/02-CONTEXT.md</read_first>
  <action>Extend `app/domain/schemas/meeting_transcript.py` with `MeetingTranscriptItemResponse` fields `block_id`, `segment_id`, `block_sequence`, `segment_index`, `speaker_label`, `text`, `start_ms`, and `end_ms`, plus `MeetingTranscriptPageResponse` fields `meeting_id`, `items`, `next_cursor`, and `has_more`. Create `app/services/meeting/transcript_service.py` with `MeetingTranscriptService` methods `append_closed_block(...)` and `get_transcript_page(...)`. `append_closed_block(...)` must accept `meeting_id`, `organization_id`, `block_id`, `sequence`, and finalized `segments[]`, then upsert only the finalized transcript segments from a closed `MeetingTranscriptBlockPayload` emitted by Plan 02-01. `get_transcript_page(...)` must return oldest-first results using a composite cursor encoded from `(block_sequence, segment_index)` with default `limit=50` and maximum `limit=200`. Update `app/services/meeting/meeting_service.py` so `MeetingSessionEventKind.TRANSCRIPT` events with `is_final=True` are persisted through `MeetingTranscriptService` before durable review reads rely on them. Wire `get_meeting_transcript_service()` into `app/common/service.py`.</action>
  <verify>Select-String -Path app/domain/schemas/meeting_transcript.py,app/services/meeting/transcript_service.py,app/services/meeting/meeting_service.py,app/common/service.py -Pattern "class MeetingTranscriptItemResponse|class MeetingTranscriptPageResponse|class MeetingTranscriptService|append_closed_block|get_transcript_page|get_meeting_transcript_service"</verify>
  <acceptance_criteria>
    - app/domain/schemas/meeting_transcript.py contains `class MeetingTranscriptItemResponse`
    - app/domain/schemas/meeting_transcript.py contains `class MeetingTranscriptPageResponse`
    - app/domain/schemas/meeting_transcript.py contains `block_id: str`
    - app/domain/schemas/meeting_transcript.py contains `segment_id: str`
    - app/domain/schemas/meeting_transcript.py contains `next_cursor: str | None`
    - app/services/meeting/transcript_service.py contains `class MeetingTranscriptService`
    - app/services/meeting/transcript_service.py contains `async def append_closed_block`
    - app/services/meeting/transcript_service.py contains `async def get_transcript_page`
    - app/services/meeting/meeting_service.py contains `is_final`
    - app/services/meeting/meeting_service.py contains `append_closed_block`
    - app/common/service.py contains `def get_meeting_transcript_service() -> MeetingTranscriptService`
  </acceptance_criteria>
  <done>Closed meeting transcript blocks are durably persisted as finalized segments and available through a service that serves paginated oldest-first transcript review.</done>
</task>

<task type="auto">
  <name>Task 3: Add meeting transcript review API and persistence/read-path tests</name>
  <files>app/api/v1/meetings/router.py, app/api/v1/router.py, tests/unit/repo/test_meeting_transcript_repo.py, tests/unit/services/test_meeting_transcript_service.py, tests/integration/api/test_meeting_transcript_api.py</files>
  <read_first>app/api/deps.py, app/api/v1/router.py, app/api/v1/organizations/routes.py, app/services/meeting/transcript_service.py, app/domain/schemas/meeting_transcript.py, .planning/phases/02-live-transcript-capture/02-VALIDATION.md</read_first>
  <action>Create `app/api/v1/meetings/router.py` with `router = APIRouter(prefix="/meetings", tags=["meetings"])` and `@router.get("/{meeting_id}/transcript", response_model=MeetingTranscriptPageResponse)`. Use `get_current_active_user` and `get_current_organization_context` so transcript review requires `Authorization` plus `X-Organization-ID`. Accept query params `limit: int = Query(default=50, ge=1, le=200)` and `cursor: str | None = Query(default=None)`, where the cursor encodes the last seen `(block_sequence, segment_index)`. Call `MeetingTranscriptService.get_transcript_page(...)` and return oldest-first items for active or completed meetings. Register the new router in `app/api/v1/router.py`. Add `tests/unit/repo/test_meeting_transcript_repo.py` for idempotent block-segment upsert and composite-cursor pagination, `tests/unit/services/test_meeting_transcript_service.py` for organization scoping and active/completed reads, and `tests/integration/api/test_meeting_transcript_api.py` asserting the endpoint returns `block_id`, `segment_id`, `speaker_label`, `start_ms`, `end_ms`, `next_cursor`, and `has_more`, rejects missing `X-Organization-ID`, and never exposes `conversation_id`.</action>
  <verify>pytest tests/unit/repo/test_meeting_transcript_repo.py tests/unit/services/test_meeting_transcript_service.py -q; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pytest tests/integration/api/test_meeting_transcript_api.py -q</verify>
  <acceptance_criteria>
    - app/api/v1/meetings/router.py contains `router = APIRouter(prefix="/meetings", tags=["meetings"])`
    - app/api/v1/meetings/router.py contains `@router.get("/{meeting_id}/transcript"`
    - app/api/v1/meetings/router.py contains `X-Organization-ID`
    - app/api/v1/router.py includes `meetings_router`
    - tests/unit/repo/test_meeting_transcript_repo.py contains `upsert_block_segments`
    - tests/unit/services/test_meeting_transcript_service.py contains `get_transcript_page`
    - tests/integration/api/test_meeting_transcript_api.py contains `next_cursor`
    - tests/integration/api/test_meeting_transcript_api.py contains `block_id`
    - tests/integration/api/test_meeting_transcript_api.py contains `segment_id`
    - tests/integration/api/test_meeting_transcript_api.py contains `speaker_label`
    - tests/integration/api/test_meeting_transcript_api.py does not require `conversation_id`
    - All listed test commands exit 0
  </acceptance_criteria>
  <done>Meeting transcript review is available through a meeting-native API surface and covered by persistence, service, and integration tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Select-String -Path app/domain/models/meeting_transcript_item.py,app/repo/meeting_transcript_repo.py,app/services/meeting/transcript_service.py,app/api/v1/meetings/router.py -Pattern "MeetingTranscriptItem|upsert_block_segments|list_by_meeting|get_transcript_page|/transcript|next_cursor"` returns matches
- [ ] `pytest tests/unit/repo/test_meeting_transcript_repo.py tests/unit/services/test_meeting_transcript_service.py -q` passes
- [ ] `pytest tests/integration/api/test_meeting_transcript_api.py -q` passes
- [ ] Transcript review payloads are oldest-first, paginated, and free of interview-only fields
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Saved meeting transcripts can be reviewed through a paginated meeting-native API
- Stable transcript segment persistence is durable, organization-scoped, and compatible with later history/review phases
  </success_criteria>

<output>
After completion, create `.planning/phases/02-live-transcript-capture/02-02-SUMMARY.md`
</output>
