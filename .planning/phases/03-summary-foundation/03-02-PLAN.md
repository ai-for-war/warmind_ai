---
phase: 03-summary-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/config/settings.py
  - .env.example
  - app/prompts/system/meeting_summary.py
  - app/repo/meeting_transcript_repo.py
  - app/domain/schemas/meeting_summary.py
  - app/services/meeting/summary_service.py
  - app/workers/meeting_summary_worker.py
  - tests/unit/services/test_meeting_summary_service.py
  - tests/unit/test_meeting_summary_worker.py
  - tests/integration/socket_gateway/test_meeting_record_summary_socket.py
autonomous: true
requirements:
  - SUMM-01
  - SUMM-07
user_setup: []
must_haves:
  truths:
    - "Live and final summary generation reads only stable transcript segments up to the requested `target_block_sequence` and never uses volatile `draft_segments`."
    - "Generated meeting summaries are always `2-3` speaker-neutral bullets in the meeting language and are emitted on the same `meeting_record:summary` surface with lifecycle states `updating`, `ready`, `finalizing`, `final_ready`, and `failed`."
    - "Finalization promotes the same short-summary surface to `is_final=True`; it never switches to a richer post-meeting schema in this phase."
  artifacts:
    - path: "app/prompts/system/meeting_summary.py"
      provides: "Meeting-summary prompt contract that constrains language, bullet count, and speaker neutrality"
      min_lines: 30
      contains: "MEETING_SUMMARY_SYSTEM_PROMPT"
    - path: "app/services/meeting/summary_service.py"
      provides: "Transcript-to-summary generation and durable state transition logic"
      min_lines: 220
      contains: "generate_summary_for_job"
    - path: "tests/integration/socket_gateway/test_meeting_record_summary_socket.py"
      provides: "End-to-end live/final summary socket lifecycle coverage"
      min_lines: 120
      contains: "meeting_record:summary"
  key_links:
    - from: "app/workers/meeting_summary_worker.py"
      to: "app/services/meeting/summary_service.py"
      via: "worker delegates transcript loading, generation, and persistence to summary service"
      pattern: "process_job|generate_summary_for_job"
    - from: "app/services/meeting/summary_service.py"
      to: "app/repo/meeting_transcript_repo.py"
      via: "summary generation loads stable transcript segments up to the queued block sequence"
      pattern: "list_up_to_block_sequence"
    - from: "app/workers/meeting_summary_worker.py"
      to: "app/socket_gateway/worker_gateway.py"
      via: "worker emits meeting summary lifecycle events to the user room"
      pattern: "MeetingRecordEvents.SUMMARY|emit_to_user"
---

<objective>
Implement live and final short-summary generation plus socket lifecycle delivery.

Purpose: Turn the summary job scaffolding from Plan 03-01 into usable batch-updated live summaries and finalized meeting summaries that respect the locked UX and content decisions.
Output: Prompted summary generation over stable transcript batches, worker-driven summary lifecycle socket events, and tests for `SUMM-01` plus `SUMM-07`.
</objective>

<execution_context>
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-summary-foundation/03-CONTEXT.md
@.planning/phases/03-summary-foundation/03-RESEARCH.md
@.planning/phases/03-summary-foundation/03-VALIDATION.md
@.planning/phases/03-summary-foundation/03-01-PLAN.md
@app/common/event_socket.py
@app/config/settings.py
@app/infrastructure/llm/factory.py
@app/repo/meeting_transcript_repo.py
@app/repo/meeting_summary_repo.py
@app/repo/meeting_summary_job_repo.py
@app/services/meeting/summary_service.py
@app/socket_gateway/worker_gateway.py
@app/workers/meeting_summary_worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement transcript-to-summary generation with strict prompt and transcript bounds</name>
  <files>app/config/settings.py, .env.example, app/prompts/system/meeting_summary.py, app/repo/meeting_transcript_repo.py, app/services/meeting/summary_service.py</files>
  <read_first>app/config/settings.py, .env.example, app/infrastructure/llm/factory.py, app/prompts/system/interview_answer.py, app/repo/meeting_transcript_repo.py, app/services/meeting/summary_service.py, .planning/phases/03-summary-foundation/03-CONTEXT.md, .planning/phases/03-summary-foundation/03-RESEARCH.md</read_first>
  <action>In `app/config/settings.py`, add `MEETING_SUMMARY_MODEL: str = "gpt-5.2"`, `MEETING_SUMMARY_MAX_INPUT_SEGMENTS: int = 120`, and `MEETING_SUMMARY_MAX_TOKENS: int = 400`, then mirror them in `.env.example`. Create `app/prompts/system/meeting_summary.py` with `MEETING_SUMMARY_SYSTEM_PROMPT` plus a helper that builds a user prompt requiring exact JSON output `{\"bullets\":[\"...\",\"...\"]}` with 2 or 3 non-empty bullet strings, speaker-neutral wording, no hallucinated facts, and language equal to the provided meeting language. Extend `MeetingTranscriptRepository` in `app/repo/meeting_transcript_repo.py` with `list_up_to_block_sequence(meeting_id, organization_id, max_block_sequence, limit)` returning oldest-first finalized transcript segments where `block_sequence <= max_block_sequence`. Extend `MeetingSummaryService` with `generate_summary_for_job(...)` and update `process_job(...)` so it loads stable transcript segments through `list_up_to_block_sequence(...)`, joins them into a chronological plain-text transcript, calls `get_chat_openai(model=settings.MEETING_SUMMARY_MODEL, temperature=0.2, streaming=False, max_tokens=settings.MEETING_SUMMARY_MAX_TOKENS, reasoning_effort=\"medium\")`, parses the JSON `bullets` array, enforces bullet count 2-3, and persists the result through `upsert_latest_summary(...)` with `status=\"ready\"` for `job_kind=\"live_refresh\"` or `status=\"final_ready\"` for `job_kind=\"finalize\"`. If the latest stored summary already has `source_block_sequence >= target_block_sequence`, `process_job(...)` must no-op and mark the job completed without overwriting newer content.</action>
  <verify>Select-String -Path app/config/settings.py,.env.example,app/prompts/system/meeting_summary.py,app/repo/meeting_transcript_repo.py,app/services/meeting/summary_service.py -Pattern "MEETING_SUMMARY_MODEL|MEETING_SUMMARY_MAX_INPUT_SEGMENTS|MEETING_SUMMARY_SYSTEM_PROMPT|bullets|list_up_to_block_sequence|generate_summary_for_job|final_ready|ready"</verify>
  <acceptance_criteria>
    - app/config/settings.py contains `MEETING_SUMMARY_MODEL: str = "gpt-5.2"`
    - app/config/settings.py contains `MEETING_SUMMARY_MAX_INPUT_SEGMENTS: int = 120`
    - app/config/settings.py contains `MEETING_SUMMARY_MAX_TOKENS: int = 400`
    - .env.example contains `MEETING_SUMMARY_MODEL=`
    - app/prompts/system/meeting_summary.py contains `MEETING_SUMMARY_SYSTEM_PROMPT`
    - app/prompts/system/meeting_summary.py contains `"bullets"`
    - app/prompts/system/meeting_summary.py contains `2 or 3`
    - app/repo/meeting_transcript_repo.py contains `async def list_up_to_block_sequence`
    - app/services/meeting/summary_service.py contains `async def generate_summary_for_job`
    - app/services/meeting/summary_service.py contains `get_chat_openai(`
    - app/services/meeting/summary_service.py contains `status="ready"`
    - app/services/meeting/summary_service.py contains `status="final_ready"`
    - app/services/meeting/summary_service.py no-ops stale jobs when a newer `source_block_sequence` already exists
  </acceptance_criteria>
  <done>Summary jobs can now deterministically build 2-3 bullet meeting summaries from stable transcript segments using the meeting language and bounded transcript inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Emit same-surface live and final summary lifecycle events from the worker</name>
  <files>app/domain/schemas/meeting_summary.py, app/services/meeting/summary_service.py, app/workers/meeting_summary_worker.py</files>
  <read_first>app/domain/schemas/meeting_summary.py, app/services/meeting/summary_service.py, app/workers/meeting_summary_worker.py, app/socket_gateway/worker_gateway.py, app/common/event_socket.py, .planning/phases/03-summary-foundation/03-CONTEXT.md</read_first>
  <action>Extend `MeetingSummaryPayload` so the worker can emit the same contract for both live and final summary states. In `MeetingSummaryWorker.process_task(...)`, load the latest stored summary before generation and emit `MeetingRecordEvents.SUMMARY` through `worker_gateway.emit_to_user(...)` with these exact state transitions: for `job_kind=\"live_refresh\"`, emit `status=\"updating\"` and `is_final=False` before generation, then emit `status=\"ready\"` and `is_final=False` after persistence; for `job_kind=\"finalize\"`, emit `status=\"finalizing\"` and `is_final=False` before generation, then emit `status=\"final_ready\"` and `is_final=True` after persistence. The pre-generation payload must preserve the last stored `bullets` so the client can keep showing the last good summary while work is running. On failures, emit `status=\"failed\"` with `error_message` and the last stored `bullets` unchanged. Mark jobs completed only after the success event is emitted; mark jobs failed before the failed event is emitted.</action>
  <verify>Select-String -Path app/domain/schemas/meeting_summary.py,app/services/meeting/summary_service.py,app/workers/meeting_summary_worker.py -Pattern "updating|ready|finalizing|final_ready|failed|MeetingRecordEvents.SUMMARY|emit_to_user|error_message|is_final"</verify>
  <acceptance_criteria>
    - app/domain/schemas/meeting_summary.py contains `status`
    - app/domain/schemas/meeting_summary.py contains `bullets: list[str]`
    - app/workers/meeting_summary_worker.py contains `MeetingRecordEvents.SUMMARY`
    - app/workers/meeting_summary_worker.py contains `status="updating"`
    - app/workers/meeting_summary_worker.py contains `status="ready"`
    - app/workers/meeting_summary_worker.py contains `status="finalizing"`
    - app/workers/meeting_summary_worker.py contains `status="final_ready"`
    - app/workers/meeting_summary_worker.py contains `status="failed"`
    - app/workers/meeting_summary_worker.py preserves previous `bullets` in the updating/finalizing payload
    - app/workers/meeting_summary_worker.py emits success only after summary persistence succeeds
  </acceptance_criteria>
  <done>The worker now drives the single meeting summary surface through updating, ready, finalizing, final-ready, and failed states without clearing the last good summary content.</done>
</task>

<task type="auto">
  <name>Task 3: Add summary generation, worker, and socket lifecycle tests</name>
  <files>tests/unit/services/test_meeting_summary_service.py, tests/unit/test_meeting_summary_worker.py, tests/integration/socket_gateway/test_meeting_record_summary_socket.py</files>
  <read_first>tests/unit/services/test_meeting_transcript_service.py, tests/integration/socket_gateway/test_meeting_record_socket.py, app/services/meeting/summary_service.py, app/workers/meeting_summary_worker.py, app/domain/schemas/meeting_summary.py, .planning/phases/03-summary-foundation/03-VALIDATION.md</read_first>
  <action>Extend `tests/unit/services/test_meeting_summary_service.py` to cover: transcript loading bounded by `target_block_sequence`, prompt language propagation, strict 2-3 bullet parsing from JSON, stale-job no-op when a newer summary already exists, and final summary promotion with `is_final=True`. Create `tests/unit/test_meeting_summary_worker.py` to cover worker lifecycle emission order for live and final jobs, preserving previous `bullets` during `updating` and `finalizing`, and failed-job emission with `error_message`. Create `tests/integration/socket_gateway/test_meeting_record_summary_socket.py` that uses fake transcript persistence plus a fake summary model response to assert the event sequence `meeting_record:summary(status=updating)` -> `meeting_record:summary(status=ready)` during active meetings and `meeting_record:summary(status=finalizing)` -> `meeting_record:summary(status=final_ready)` after stop, while confirming payloads expose `meeting_id`, `status`, `bullets`, `is_final`, `language`, and `source_block_sequence`.</action>
  <verify>pytest tests/unit/services/test_meeting_summary_service.py tests/unit/test_meeting_summary_worker.py -q; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pytest tests/integration/socket_gateway/test_meeting_record_summary_socket.py -q</verify>
  <acceptance_criteria>
    - tests/unit/services/test_meeting_summary_service.py contains `generate_summary_for_job`
    - tests/unit/services/test_meeting_summary_service.py contains `source_block_sequence`
    - tests/unit/services/test_meeting_summary_service.py contains `is_final`
    - tests/unit/test_meeting_summary_worker.py contains `updating`
    - tests/unit/test_meeting_summary_worker.py contains `finalizing`
    - tests/unit/test_meeting_summary_worker.py contains `failed`
    - tests/integration/socket_gateway/test_meeting_record_summary_socket.py contains `meeting_record:summary`
    - tests/integration/socket_gateway/test_meeting_record_summary_socket.py contains `final_ready`
    - tests/integration/socket_gateway/test_meeting_record_summary_socket.py contains `source_block_sequence`
    - tests/integration/socket_gateway/test_meeting_record_summary_socket.py contains `bullets`
    - All listed test commands exit 0
  </acceptance_criteria>
  <done>Phase 3 summary generation and lifecycle behavior is covered at the service, worker, and socket-runtime layers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Select-String -Path app/prompts/system/meeting_summary.py,app/repo/meeting_transcript_repo.py,app/services/meeting/summary_service.py,app/workers/meeting_summary_worker.py,tests/unit/test_meeting_summary_worker.py,tests/integration/socket_gateway/test_meeting_record_summary_socket.py -Pattern "MEETING_SUMMARY_SYSTEM_PROMPT|list_up_to_block_sequence|generate_summary_for_job|meeting_record:summary|updating|final_ready"` returns matches
- [ ] `pytest tests/unit/services/test_meeting_summary_service.py tests/unit/test_meeting_summary_worker.py -q` passes
- [ ] `pytest tests/integration/socket_gateway/test_meeting_record_summary_socket.py -q` passes
- [ ] Live and final summary payloads reuse the same schema and keep the last good summary visible during processing states
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can receive live batch-updated short summaries and a final short summary on the same surface
- Summary generation is stable, language-aware, speaker-neutral, and driven only by stable transcript batches
  </success_criteria>

<output>
After completion, create `.planning/phases/03-summary-foundation/03-02-SUMMARY.md`
</output>
