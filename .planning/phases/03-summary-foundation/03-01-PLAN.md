---
phase: 03-summary-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config/settings.py
  - .env.example
  - app/common/event_socket.py
  - app/domain/models/meeting_summary.py
  - app/domain/models/meeting_summary_job.py
  - app/domain/schemas/meeting_summary.py
  - app/repo/meeting_summary_repo.py
  - app/repo/meeting_summary_job_repo.py
  - app/common/repo.py
  - app/infrastructure/database/mongodb.py
  - app/services/meeting/summary_service.py
  - app/services/meeting/meeting_service.py
  - app/common/service.py
  - app/workers/meeting_summary_worker.py
  - scripts/system/worker_meeting_summary.sh
  - tests/unit/repo/test_meeting_summary_repo.py
  - tests/unit/services/test_meeting_summary_service.py
autonomous: true
requirements:
  - SUMM-01
  - SUMM-07
user_setup: []
must_haves:
  truths:
    - "Meeting summaries use a dedicated durable summary document keyed by `meeting_id`, separate from transcript storage, with `bullets`, `status`, `is_final`, `language`, and `source_block_sequence`."
    - "Summary jobs are durable and idempotent by `meeting_id + job_kind + target_block_sequence`, so repeated transcript close/finalize events do not create duplicate work."
    - "Live summary work is only enqueued after finalized transcript block persistence succeeds and only when block-growth thresholds are met; final summary work is enqueued only after the last finalized transcript block is durably persisted."
  artifacts:
    - path: "app/domain/models/meeting_summary.py"
      provides: "Durable latest-summary state per meeting"
      min_lines: 35
      contains: "class MeetingSummary"
    - path: "app/repo/meeting_summary_job_repo.py"
      provides: "Durable summary job dedup, claim, and terminal-state persistence"
      min_lines: 90
      contains: "class MeetingSummaryJobRepository"
    - path: "app/services/meeting/summary_service.py"
      provides: "Debounce, enqueue, and summary job orchestration entrypoints"
      min_lines: 130
      contains: "class MeetingSummaryService"
  key_links:
    - from: "app/services/meeting/meeting_service.py"
      to: "app/services/meeting/summary_service.py"
      via: "meeting transcript persistence success triggers live/final summary enqueue decisions"
      pattern: "maybe_enqueue_live_summary|enqueue_final_summary"
    - from: "app/services/meeting/summary_service.py"
      to: "app/repo/meeting_summary_job_repo.py"
      via: "service creates idempotent pending jobs before queueing Redis work"
      pattern: "create_or_get_pending_job|claim_pending_job"
    - from: "app/workers/meeting_summary_worker.py"
      to: "app/services/meeting/summary_service.py"
      via: "worker claims summary jobs and delegates processing orchestration to summary service"
      pattern: "process_job"
---

<objective>
Build the durable summary lane and job orchestration for meeting summaries.

Purpose: Establish the dedicated summary contracts, persistence, queue settings, debounce/enqueue behavior, and worker shell needed to generate live/final short summaries on stable transcript batches.
Output: Meeting summary models/schemas/repos, summary queue/job plumbing, meeting-service enqueue hooks after transcript persistence, and a worker entrypoint ready for generation logic in Plan 03-02.
</objective>

<execution_context>
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Project/AI_FOR_WAR/ai_service_kiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-summary-foundation/03-CONTEXT.md
@.planning/phases/03-summary-foundation/03-RESEARCH.md
@.planning/phases/03-summary-foundation/03-VALIDATION.md
@app/config/settings.py
@app/common/event_socket.py
@app/common/repo.py
@app/common/service.py
@app/infrastructure/database/mongodb.py
@app/infrastructure/redis/redis_queue.py
@app/repo/meeting_record_repo.py
@app/repo/meeting_transcript_repo.py
@app/services/meeting/meeting_service.py
@app/services/meeting/transcript_service.py
@app/workers/image_generation_worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create durable summary contracts, repositories, indexes, and queue settings</name>
  <files>app/config/settings.py, .env.example, app/common/event_socket.py, app/domain/models/meeting_summary.py, app/domain/models/meeting_summary_job.py, app/domain/schemas/meeting_summary.py, app/repo/meeting_summary_repo.py, app/repo/meeting_summary_job_repo.py, app/common/repo.py, app/infrastructure/database/mongodb.py</files>
  <read_first>app/config/settings.py, .env.example, app/common/event_socket.py, app/domain/models/meeting_record.py, app/domain/schemas/meeting_record.py, app/repo/meeting_record_repo.py, app/repo/meeting_transcript_repo.py, app/common/repo.py, app/infrastructure/database/mongodb.py, .planning/phases/03-summary-foundation/03-CONTEXT.md, .planning/phases/03-summary-foundation/03-RESEARCH.md</read_first>
  <action>In `app/config/settings.py`, add `MEETING_SUMMARY_QUEUE_NAME: str = "meeting_summary_tasks"`, `MEETING_SUMMARY_MAX_CONCURRENCY: int = 2`, `MEETING_SUMMARY_MIN_BLOCKS_FOR_LIVE: int = 2`, and `MEETING_SUMMARY_MIN_NEW_BLOCKS: int = 2`, then mirror those exact env vars in `.env.example`. In `app/common/event_socket.py`, add `MeetingRecordEvents.SUMMARY = "meeting_record:summary"`. Create `app/domain/models/meeting_summary.py` with a `MeetingSummary` model containing `_id`, `meeting_id`, `organization_id`, `language`, `status`, `bullets: list[str]`, `is_final`, `source_block_sequence`, `error_message`, `created_at`, and `updated_at`. Create `app/domain/models/meeting_summary_job.py` with a `MeetingSummaryJob` model containing `_id`, `meeting_id`, `organization_id`, `user_id`, `job_kind`, `target_block_sequence`, `status`, `retry_count`, `queued_at`, `started_at`, `completed_at`, and `error_message`. Create `app/domain/schemas/meeting_summary.py` with `MeetingSummaryPayload` fields `meeting_id`, `status`, `bullets`, `is_final`, `language`, `source_block_sequence`, and `error_message`. Create `MeetingSummaryRepository` in `app/repo/meeting_summary_repo.py` with methods `get_latest_by_meeting(...)`, `upsert_latest_summary(...)`, and `mark_status(...)` over a `meeting_summaries` collection keyed by `meeting_id`. Create `MeetingSummaryJobRepository` in `app/repo/meeting_summary_job_repo.py` with methods `create_or_get_pending_job(...)`, `claim_pending_job(...)`, `mark_completed(...)`, `mark_failed(...)`, and `get_latest_job(...)` over a `meeting_summary_jobs` collection. Add `get_meeting_summary_repo()` and `get_meeting_summary_job_repo()` to `app/common/repo.py`. Extend `app/infrastructure/database/mongodb.py` to create these indexes: unique `idx_meeting_summaries_meeting_unique` on `(meeting_id)`, read index `idx_meeting_summaries_org_final_updated` on `(organization_id, is_final, updated_at DESC)`, unique `idx_meeting_summary_jobs_unique_target` on `(meeting_id, job_kind, target_block_sequence)`, and worker queue index `idx_meeting_summary_jobs_status_queued` on `(status, queued_at ASC)`.</action>
  <verify>Select-String -Path app/config/settings.py,.env.example,app/common/event_socket.py,app/domain/models/meeting_summary.py,app/domain/models/meeting_summary_job.py,app/domain/schemas/meeting_summary.py,app/repo/meeting_summary_repo.py,app/repo/meeting_summary_job_repo.py,app/common/repo.py,app/infrastructure/database/mongodb.py -Pattern "MEETING_SUMMARY_QUEUE_NAME|MEETING_SUMMARY_MIN_BLOCKS_FOR_LIVE|meeting_record:summary|class MeetingSummary|class MeetingSummaryJob|class MeetingSummaryPayload|class MeetingSummaryRepository|class MeetingSummaryJobRepository|idx_meeting_summaries_meeting_unique|idx_meeting_summary_jobs_unique_target"</verify>
  <acceptance_criteria>
    - app/config/settings.py contains `MEETING_SUMMARY_QUEUE_NAME: str = "meeting_summary_tasks"`
    - app/config/settings.py contains `MEETING_SUMMARY_MAX_CONCURRENCY: int = 2`
    - app/config/settings.py contains `MEETING_SUMMARY_MIN_BLOCKS_FOR_LIVE: int = 2`
    - app/config/settings.py contains `MEETING_SUMMARY_MIN_NEW_BLOCKS: int = 2`
    - .env.example contains `MEETING_SUMMARY_QUEUE_NAME=`
    - .env.example contains `MEETING_SUMMARY_MAX_CONCURRENCY=`
    - app/common/event_socket.py contains `SUMMARY = "meeting_record:summary"`
    - app/domain/models/meeting_summary.py contains `class MeetingSummary`
    - app/domain/models/meeting_summary.py contains `bullets: list[str]`
    - app/domain/models/meeting_summary_job.py contains `class MeetingSummaryJob`
    - app/domain/models/meeting_summary_job.py contains `target_block_sequence`
    - app/domain/schemas/meeting_summary.py contains `class MeetingSummaryPayload`
    - app/repo/meeting_summary_repo.py contains `async def upsert_latest_summary`
    - app/repo/meeting_summary_job_repo.py contains `async def create_or_get_pending_job`
    - app/common/repo.py contains `def get_meeting_summary_repo() -> MeetingSummaryRepository`
    - app/common/repo.py contains `def get_meeting_summary_job_repo() -> MeetingSummaryJobRepository`
    - app/infrastructure/database/mongodb.py contains `idx_meeting_summaries_meeting_unique`
    - app/infrastructure/database/mongodb.py contains `idx_meeting_summary_jobs_unique_target`
  </acceptance_criteria>
  <done>Meeting summary state, job dedup, socket event contract, queue configuration, and required indexes exist as meeting-native primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Add debounce-aware enqueue orchestration after stable transcript persistence</name>
  <files>app/services/meeting/summary_service.py, app/services/meeting/meeting_service.py, app/common/service.py</files>
  <read_first>app/services/meeting/meeting_service.py, app/services/meeting/transcript_service.py, app/repo/meeting_transcript_repo.py, app/repo/meeting_summary_repo.py, app/repo/meeting_summary_job_repo.py, app/common/service.py, app/config/settings.py, .planning/phases/03-summary-foundation/03-CONTEXT.md, .planning/phases/03-summary-foundation/03-RESEARCH.md</read_first>
  <action>Create `app/services/meeting/summary_service.py` with `MeetingSummaryService` methods `maybe_enqueue_live_summary(...)`, `enqueue_final_summary(...)`, `get_latest_summary(...)`, and `process_job(...)`. Implement live-threshold logic so `maybe_enqueue_live_summary(...)` only queues work when `target_block_sequence + 1 >= settings.MEETING_SUMMARY_MIN_BLOCKS_FOR_LIVE` and the new block sequence is at least `settings.MEETING_SUMMARY_MIN_NEW_BLOCKS` ahead of the latest queued or stored live summary sequence. `enqueue_final_summary(...)` must always create or reuse a `job_kind="finalize"` job for the latest persisted target sequence. Both methods must create durable job records through `MeetingSummaryJobRepository.create_or_get_pending_job(...)` before calling `RedisQueue.enqueue(...)` with payload keys `job_id`, `meeting_id`, `organization_id`, `user_id`, `job_kind`, and `target_block_sequence`. Update `app/services/meeting/meeting_service.py` so `_schedule_transcript_persistence(...)` and `_persist_transcript_block_in_background(...)` receive `language` and `finalize_summary` arguments. After `append_closed_block_payload(...)` succeeds, call `summary_service.maybe_enqueue_live_summary(...)` for normal closed blocks and `summary_service.enqueue_final_summary(...)` for finalizing blocks so summary jobs are created only after transcript durability succeeds. Wire `get_meeting_summary_service()` into `app/common/service.py` using `get_redis_queue()`, `get_meeting_summary_repo()`, `get_meeting_summary_job_repo()`, `get_meeting_transcript_repo()`, and `get_meeting_record_repo()`.</action>
  <verify>Select-String -Path app/services/meeting/summary_service.py,app/services/meeting/meeting_service.py,app/common/service.py -Pattern "class MeetingSummaryService|maybe_enqueue_live_summary|enqueue_final_summary|process_job|get_meeting_summary_service|finalize_summary|MEETING_SUMMARY_MIN_NEW_BLOCKS|target_block_sequence"</verify>
  <acceptance_criteria>
    - app/services/meeting/summary_service.py contains `class MeetingSummaryService`
    - app/services/meeting/summary_service.py contains `async def maybe_enqueue_live_summary`
    - app/services/meeting/summary_service.py contains `async def enqueue_final_summary`
    - app/services/meeting/summary_service.py contains `async def process_job`
    - app/services/meeting/summary_service.py contains `MEETING_SUMMARY_MIN_BLOCKS_FOR_LIVE`
    - app/services/meeting/summary_service.py contains `MEETING_SUMMARY_MIN_NEW_BLOCKS`
    - app/services/meeting/summary_service.py enqueues keys `job_id`, `meeting_id`, `organization_id`, `user_id`, `job_kind`, and `target_block_sequence`
    - app/services/meeting/meeting_service.py passes `language=`
    - app/services/meeting/meeting_service.py passes `finalize_summary=`
    - app/services/meeting/meeting_service.py contains `maybe_enqueue_live_summary`
    - app/services/meeting/meeting_service.py contains `enqueue_final_summary`
    - app/common/service.py contains `def get_meeting_summary_service() -> MeetingSummaryService`
  </acceptance_criteria>
  <done>Transcript persistence success now drives idempotent live/final summary job creation with explicit debounce thresholds and no direct LLM work in the socket path.</done>
</task>

<task type="auto">
  <name>Task 3: Create the meeting summary worker shell and orchestration tests</name>
  <files>app/workers/meeting_summary_worker.py, scripts/system/worker_meeting_summary.sh, tests/unit/repo/test_meeting_summary_repo.py, tests/unit/services/test_meeting_summary_service.py</files>
  <read_first>app/workers/image_generation_worker.py, scripts/system/worker_image_generation.sh, app/socket_gateway/worker_gateway.py, app/repo/meeting_summary_job_repo.py, app/services/meeting/summary_service.py, .planning/phases/03-summary-foundation/03-VALIDATION.md</read_first>
  <action>Create `app/workers/meeting_summary_worker.py` following the image worker structure: define `MeetingSummaryTask.from_dict(...)`, `MeetingSummaryWorker.DEQUEUE_TIMEOUT = 5`, `run_once()`, `_run_task(...)`, `process_task(...)`, `start()`, and `stop()`. `process_task(...)` must claim the job through `MeetingSummaryJobRepository.claim_pending_job(...)`, call `MeetingSummaryService.process_job(...)`, and mark failed jobs through `mark_failed(...)` on unhandled exceptions. Add `scripts/system/worker_meeting_summary.sh` containing exactly `python -m app.workers.meeting_summary_worker`. Add `tests/unit/repo/test_meeting_summary_repo.py` covering `upsert_latest_summary(...)` overwrite semantics and `create_or_get_pending_job(...)` idempotency for the same `(meeting_id, job_kind, target_block_sequence)`. Add `tests/unit/services/test_meeting_summary_service.py` covering the live-threshold rules, duplicate-job suppression, and final-job creation after finalized transcript persistence. Keep `MeetingSummaryService.process_job(...)` as the orchestration entrypoint that Plan 03-02 will complete with actual LLM generation; the Plan 03-01 tests should validate queue/job creation behavior rather than summary text output.</action>
  <verify>pytest tests/unit/repo/test_meeting_summary_repo.py tests/unit/services/test_meeting_summary_service.py -q</verify>
  <acceptance_criteria>
    - app/workers/meeting_summary_worker.py contains `class MeetingSummaryTask`
    - app/workers/meeting_summary_worker.py contains `class MeetingSummaryWorker`
    - app/workers/meeting_summary_worker.py contains `DEQUEUE_TIMEOUT = 5`
    - app/workers/meeting_summary_worker.py contains `claim_pending_job`
    - app/workers/meeting_summary_worker.py contains `process_job`
    - scripts/system/worker_meeting_summary.sh contains `python -m app.workers.meeting_summary_worker`
    - tests/unit/repo/test_meeting_summary_repo.py contains `create_or_get_pending_job`
    - tests/unit/repo/test_meeting_summary_repo.py contains `upsert_latest_summary`
    - tests/unit/services/test_meeting_summary_service.py contains `maybe_enqueue_live_summary`
    - tests/unit/services/test_meeting_summary_service.py contains `enqueue_final_summary`
    - All listed test commands exit 0
  </acceptance_criteria>
  <done>The repo has a runnable meeting-summary worker entrypoint plus unit coverage for the durable enqueue and dedup logic that Phase 3 depends on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Select-String -Path app/domain/models/meeting_summary.py,app/domain/models/meeting_summary_job.py,app/repo/meeting_summary_repo.py,app/repo/meeting_summary_job_repo.py,app/services/meeting/summary_service.py,app/workers/meeting_summary_worker.py -Pattern "MeetingSummary|MeetingSummaryJob|upsert_latest_summary|create_or_get_pending_job|maybe_enqueue_live_summary|enqueue_final_summary|process_job"` returns matches
- [ ] `pytest tests/unit/repo/test_meeting_summary_repo.py tests/unit/services/test_meeting_summary_service.py -q` passes
- [ ] No queue payload is enqueued without a durable job row
- [ ] Final summary jobs are only enqueued after final transcript persistence succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Durable summary/job primitives exist with idempotent queueing
- Meeting transcript persistence now schedules live/final summary work without invoking the LLM in the socket request path
  </success_criteria>

<output>
After completion, create `.planning/phases/03-summary-foundation/03-01-SUMMARY.md`
</output>
